#include <iostream>
#include <fstream>
#include <cctype>
using namespace std;

int main(){

enum COL { L = 0, D, U, NLDU, A, R, T };
enum state { f = 0, t };
enum lu { n = 0, id, op, pun, key };


char len[100];
int li = 0;
state currentState = f;
char cha;

COL coltype;
lu lexUnit;


    return 1;

}